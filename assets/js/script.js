

// Alternar entre modo develop e production:
const MODE = 'dev' // 'prod'

const alter = (mode)=>{
    if(mode == 'dev'){
    return 1
}}

alter(MODE)

console.log(alter(MODE))


// Implementar uma função para que ao mudar de dev para prod o link do CSS muda, 
// ../assets/css/style.css
//afim de facilitar a construção do código e a visualização em tempo real

